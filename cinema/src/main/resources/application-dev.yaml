# Development profile configuration
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.cinema: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
